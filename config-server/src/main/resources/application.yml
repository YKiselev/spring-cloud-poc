server:
  port: 8888
spring:
  profiles:
    active: native, dev
#  config:
#    name: foo
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/configs/{profile}
management:
  security:
    enabled: false